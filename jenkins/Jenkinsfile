pipeline {
    agent any
    environment {
        CI = "true"
    }
    stages {
        stage('Add Deps') {
             steps {
                sh './jenkins/scripts/install-deps.sh'
             }
        }
        stage('Test'){
            steps {
                sh './jenkins/scripts/test.sh'
            }
        }
        stage('Build'){
            steps {
                sh './jenkins/scripts/build.sh'
            }
        }
    }
}